import { GlobalSearchClient } from '@/components/search/GlobalSearchClient';

export const metadata = {
  title: 'Search App Data | Made4U Flow',
  description: 'Search across products, machinery, prep steps, production schedules, staffing, and storage in Made4U Flow.',
};

export default function SearchPage() {
  return <GlobalSearchClient />;
}
