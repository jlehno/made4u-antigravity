# Project Rules

- **Automatic GitHub Sync on Deployment**: Whenever the user asks to deploy the app to Firebase, always commit the latest code changes (`git commit`) and push them to GitHub (`git push origin main`) as part of the deployment workflow.
